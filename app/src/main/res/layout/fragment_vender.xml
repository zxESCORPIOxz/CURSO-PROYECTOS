<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/banner"
    tools:context=".ui.vender.Vender">
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/lista_mis_productos"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btn_agregar_publicacion"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        app:borderWidth="0dp"
        android:backgroundTint="@color/colorVerde2"
        android:layout_margin="20dp"
        android:src="@drawable/ic_add_publicacion" />
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btn_refresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        app:borderWidth="0dp"
        android:backgroundTint="@color/colorAmarillo"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="85dp"
        android:src="@drawable/ic_refresh" />
</androidx.coordinatorlayout.widget.CoordinatorLayout>